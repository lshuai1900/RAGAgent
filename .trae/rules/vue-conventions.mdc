---
description: Vue 3 组合式 API 与 TypeScript 编码规范
globs: "web/**"
---

# vue-conventions

## 适用范围
`web/` 前端工程的所有 `.vue` 与 `.ts` 文件。

## 允许行为
- 使用 Vue 3 `<script setup lang="ts">` Composition API
- 使用 TypeScript strict 模式，所有 props/emits/ref 显式类型注解
- 组件命名：PascalCase 文件名（如 `KbCard.vue`、`CreateKbModal.vue`）
- 路径别名 `@/` → `src/`
- props 用 `defineProps<{ ... }>()` 泛型定义
- emits 用 `defineEmits<{ ... }>()` 泛型定义
- 响应式状态优先 `ref` / `reactive`，计算属性用 `computed`
- 副作用用 `watch` / `watchEffect`，生命周期用 `onMounted` / `onUnmounted` 等
- 组件按业务域分子目录（`components/knowledge-base/` / `components/document/` / `components/chat/`）

## 文件结构约定
- 页面级组件放 `views/`，与路由一一对应
- 可复用组件放 `components/{业务域}/`
- 组合式函数放 `composables/`，命名 `useXxx.ts`
- 纯工具函数放 `utils/`
- 类型定义放 `types/`
- API 客户端放 `api/`
- Pinia store 放 `stores/`

## props/emits 类型示例
```vue
<script setup lang="ts">
interface Props {
  kbId: string
  name: string
  documentCount?: number
}
const props = defineProps<Props>()

interface Emits {
  (e: 'click', kbId: string): void
  (e: 'edit', kbId: string): void
}
const emit = defineEmits<Emits>()
</script>
```

## 分层职责硬约束
- `views/` 不直接调 `api/`，必须经 store 或 composables
- `components/` 不直接调 `api/`，通过 props 接收数据 + emits 抛事件
- `utils/` 不调 api、不访问 store（纯函数）
- `stores/` 只调 `api/`，不直接发 fetch
- `api/` 只负责请求与响应解析，不持有业务状态

## 禁止行为
- ❌ 禁止使用 React 写法（JSX/useState/useEffect 等）
- ❌ 禁止使用 Options API（`data`/`methods`/`computed` 选项式）
- ❌ 禁止使用 Vue 2 风格的 `this.$emit` / `this.$refs`
- ❌ 禁止在 `<script setup>` 中使用 `defineProps` 运行时默认值对象堆砌（用泛型 + withDefaults）
- ❌ 禁止在 views 中硬编码接口路径
- ❌ 禁止在组件中直接调用 fetch（必须经 api 层）
- ❌ 禁止用 `any` 规避类型（确需时用 `unknown` + 类型守卫）
- ❌ 禁止使用 `v-html` 渲染后端返回内容（XSS 风险，除非明确信任源）

## 命名规范
- 组件文件：PascalCase（`KbCard.vue`）
- composable：`useXxx.ts`（camelCase 前缀 use）
- store：`xxx.ts`，导出 `useXxxStore`
- 类型：PascalCase（`KnowledgeBaseOut`）
- 常量：UPPER_SNAKE_CASE（`API_BASE_URL_KEY`）
- 事件名：kebab-case（`@click-item`）

## 注释与可读性
- 复杂逻辑加中文注释说明"为什么"，不注释"是什么"
- 文件顶部用简短中文说明该文件职责
