---
description: domain 纯领域层规范
globs: "src/ragent/domain/**/*.py"
---

# domain-model

## 适用范围
`src/ragent/domain/**/*.py`（纯领域层）。

## 允许内容
domain 只允许包含三个文件：

### enums.py
- DocumentStatus（pending/parsing/chunking/embedding/indexing/completed/failed）
- ChunkEmbeddingStatus（pending/embedded/indexed/failed）
- MessageRole（user/assistant）
- ProviderType（qwen/openai/deepseek）
- KnowledgeBaseStatus（active/archived）

### dto.py
跨层共享值对象（用 dataclass 或 Pydantic BaseModel）：
- ParsedDocument（text, sections, metadata）
- Section（content, page, heading）
- ChunkDraft（content, chunk_index, metadata）
- VectorRecord（id, vector, content, metadata）
- VectorHit（id, score, content, metadata）
- RetrievalResult（chunk_id, document_id, kb_id, content, score, metadata, retrieval_channel, rerank_score）
- ChatChunk（delta, finish_reason）
- ChatMessage（role, content）（领域消息结构，非 ORM）

### value_objects.py
- ChunkConfig（chunk_size, overlap, separators）
- ChatOptions（temperature, top_p, max_tokens, stop）
- RetrievalQuery（text, kb_id, session_id）

## 允许行为
- 使用 `dataclass` 或 `pydantic.BaseModel` 定义值对象
- 使用 `enum.Enum` 或 `enum.StrEnum` 定义枚举
- 使用 `from __future__ import annotations`

## 禁止行为
- ❌ **禁止 import schemas**（API Schema 在顶层 schemas/，不属 domain）
- ❌ **禁止 import persistence**（ORM Model 在 persistence/models/）
- ❌ **禁止 import infra_ai**
- ❌ **禁止 import rag**
- ❌ **禁止 import ingestion**
- ❌ **禁止 import api**
- ❌ **禁止 import framework**（domain 零业务依赖）
- ❌ 禁止在 domain 中定义 SQLAlchemy ORM Model
- ❌ 禁止在 domain 中定义 FastAPI 路由
- ❌ 禁止在 domain 中写业务逻辑（只放数据结构定义）
- ❌ 禁止在 domain 中写 IO 操作（DB/HTTP 调用）

## 硬约束
- domain 是纯领域层，**零业务依赖**
- domain 不得被 schemas 反向依赖（schemas 可依赖 domain，反向禁止）
- domain 的值对象可被 infra_ai/rag/ingestion/persistence/service 共享
- domain 的 ChatMessage（领域消息结构）与 persistence/models/chat_message.py（ORM Model）严格区分
