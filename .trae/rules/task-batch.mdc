---
description: 三批任务推进纪律
globs: "**"
---

# task-batch

## 适用范围
本项目所有编码工作，约束三批任务的推进节奏。

## 三批任务划分

| 批次 | 任务 | 范围 |
|---|---|---|
| 第 0 步 | 文档与规则落地 | AGENTS.md + docs/ + .trae/rules/（不写业务代码） |
| 第一批 | T1-T4 | 项目骨架与基础设施（依赖/配置/DB Model/迁移/异常响应/health） |
| 第二批 | T5-T9 | 文档摄取链路（parser/chunker/embedding/milvus/pipeline + KB/文档 API） |
| 第三批 | T10-T12、T14-T15、T18 | 检索与流式问答端到端（retriever/llm/prompt/chat_service/sse/e2e） |

## 允许行为
- 严格按照批次顺序推进
- 每批完成后做验收检查（对照 docs/ACCEPTANCE_CRITERIA.md）
- 验收通过后才启动下一批
- 每批完成后按 git-commit 规范提交
- 在当前批次范围内，可自由调整任务实现顺序

## 禁止行为
- ❌ **禁止一次性铺开 T1-T18**（必须分批）
- ❌ **禁止提前实现后续批次功能**（如第一批不得写 parser/chunker）
- ❌ **禁止实现暂缓项**（详见 AGENTS.md 第 7 节暂缓清单）
- ❌ 禁止跨批次提交（每批验收通过才提交，再启动下一批）
- ❌ 禁止跳过验收直接进入下一批

## 硬约束
- **当前批次确认**：每次编码开始前必须确认当前批次与任务编号
- **范围拦截**：若用户要求实现超出当前批次范围的功能，Trae 应主动拒绝并提示当前批次范围
- **暂缓项拦截**：若用户要求实现暂缓项，Trae 应立即停止并说明该功能属于暂缓项
- **验收对照**：每批完成后输出新增文件清单 + 运行命令 + 验收结果（对照 ACCEPTANCE_CRITERIA.md）

## 暂缓项清单（MVP 三批内禁止实现）
- MCP 工具集成
- 意图识别（树形分类 / IntentResolver）
- 多路检索（IntentDirected / VectorGlobal）
- 查询改写
- Rerank 模型（非 Identity）
- 三态熔断器 + 模型路由降级
- 队列式限流
- JWT 认证
- 复杂 Trace Span 树 / @rag_trace_node / 可视化追踪
- Memory summarize 摘要压缩
- PPT / HTML / unstructured 解析
- Celery / Dramatiq / Redis Queue 任务队列
- 管理后台 API（除 KB/文档 CRUD 外）
- 对话标题生成
- 多知识库路由

## 各批开始条件
| 批次 | 开始条件 |
|---|---|
| 第 0 步 | 终版设计确认 |
| 第一批 | 第 0 步验收通过 |
| 第二批 | 第一批 T1-T4 验收通过 |
| 第三批 | 第二批 T5-T9 验收通过 |
