---
description: Python 编码规范
globs: "**/*.py"
---

# python-coding

## 适用范围
所有 Python 源文件（src/ragent/**/*.py、tests/**/*.py、scripts/**/*.py）。

## 允许行为
- 使用 Python 3.11+ 特性：match 语句、TaskGroup、ExceptionGroup、type X = 语法
- 使用类型注解（函数签名、变量声明、dataclass 字段强制注解）
- 使用 dataclass 或 Pydantic BaseModel 定义数据结构
- 使用 `from __future__ import annotations` 延迟注解求值（如需）
- import 顺序：标准库 → 第三方 → 本项目（ruff isort 自动排序）
- 行宽 120 字符
- 使用 4 空格缩进
- 使用双引号字符串（ruff format 一致）

## 禁止行为
- ❌ 禁止使用 `print()` 调试（必须用 `framework/core/logging.py` 的 logger）
- ❌ 禁止使用 `time.sleep()`（用 `asyncio.sleep()`）
- ❌ 禁止使用裸 `except:`（必须捕获具体异常）
- ❌ 禁止使用 `import *`（明确导入）
- ❌ 禁止使用可变默认参数（如 `def f(x=[])`）
- ❌ 禁止使用全局可变状态（用 contextvars）
- ❌ 禁止使用 `eval`/`exec`/`__import__`
- ❌ 禁止行宽超过 120 字符
- ❌ 禁止使用 tab 缩进（必须 4 空格）
- ❌ 禁止忽略 mypy 类型错误（禁止 `# type: ignore` 无注释，必须说明原因）

## 硬约束
- 所有公共函数/类必须有类型注解
- 所有公共类必须有 docstring
- 复杂逻辑必须有注释说明 why（不是 what）
- 文件末尾必须有空行
- 模块级常量用 UPPER_SNAKE_CASE
- 类名用 PascalCase，函数/变量用 snake_case
