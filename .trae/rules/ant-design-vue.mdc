---
description: Ant Design Vue 组件使用规范
globs: "web/**"
---

# ant-design-vue

## 适用范围
`web/` 前端工程的组件选型与样式。

## 允许行为
- 优先使用 Ant Design Vue 4 组件
- 常用组件白名单：
  - 布局：`Layout` / `Layout.Header` / `Layout.Content`
  - 导航：`Menu` / `Breadcrumb`
  - 数据展示：`Card` / `Table` / `Tabs` / `Tag` / `Empty` / `Spin` / `Alert` / `Descriptions`
  - 表单：`Form` / `FormItem` / `Input` / `InputPassword` / `Textarea` / `Select` / `InputNumber` / `Switch` / `Radio`
  - 反馈：`Modal` / `message` / `notification` / `Spin`
  - 操作：`Button` / `Upload` / `Dropdown`
  - 图标：`lucide-vue-next`（与 Antd 图标二选一，统一用 lucide-vue-next）

## 状态色统一（硬约束）
状态 Tag 颜色必须与 `frontend-ui.mdc` 映射一致：

| 语义 | Tag color |
|---|---|
| 待处理 / 已归档 / 未知 | `default` |
| 处理中（解析/分块/向量化/入库） | `processing` |
| 已完成 / 启用 | `success` |
| 失败 | `error` |

不得自定义新的状态色，不得用 `warning` 表示文档处理中状态。

## 组件使用约束
- `Table`：用 `size="small"`，斑马纹关闭，列名全中文
- `Card`：白底 + 细边框，hover 边框变蓝，无抬升动画
- `Modal`：标题与按钮全中文（确定/取消）
- `Form`：用 `layout="vertical"` 或 `horizontal`，label 全中文，校验提示全中文
- `Upload`：用 `beforeUpload` 拦截做大小校验，不直接走 Antd Upload 的内置上传（统一用自定义 fetch 上传）
- `message`：成功 `message.success`，失败 `message.error`（含追踪编号）
- `Empty`：配中文 `description`

## 样式约束
- 不自定义一套设计 Token 覆盖 Antd 主题（MVP 用默认主题）
- 局部样式用 `<style scoped>` 或 CSS Module
- 全局轻量调整放 `src/assets/styles/global.css`
- 圆角统一 6px，边框 `1px solid #f0f0f0`

## 禁止行为
- ❌ 禁止引入其他 UI 框架（Element Plus / Naive UI / Vuetify / Arco Design）
- ❌ 禁止用 Tailwind CSS 作为主样式方案
- ❌ 禁止自定义一套设计 Token 覆盖 Antd 主题
- ❌ 禁止用 `warning` 色表示文档处理中状态（统一用 `processing`）
- ❌ 禁止组件库版本混用（锁定 ant-design-vue 4.x）
- ❌ 禁止直接用 Antd Upload 的 `action` 属性上传（必须自定义 fetch，便于 baseUrl 与错误处理）
