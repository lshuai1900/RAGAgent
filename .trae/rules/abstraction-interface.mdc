---
description: 抽象接口与实现类依赖规范
globs: "src/ragent/**/base.py,src/ragent/infra_ai/**/*.py,src/ragent/rag/**/*.py,src/ragent/ingestion/**/*.py"
---

# abstraction-interface

## 适用范围
所有抽象接口（base.py）与实现类文件：
- `infra_ai/llm/base.py` + `openai_compat.py`
- `infra_ai/embedding/base.py` + `openai_compat.py`
- `infra_ai/rerank/base.py` + `identity.py`
- `rag/vector/base.py` + `milvus.py`
- `rag/retrieve/base.py` + `vector_retriever.py`
- `rag/retrieve/postprocessor/base.py` + `dedup.py`
- `rag/memory/base.py` + `db_store.py`
- `ingestion/parser/base.py` + 实现
- `ingestion/chunker/base.py` + 实现

## 允许行为
- 抽象接口用 `typing.Protocol` 或 `abc.ABC` 定义
- base 接口只依赖 `domain`（domain.dto / domain.value_objects / domain.enums）
- 实现类放在同包具体文件（如 `openai_compat.py`、`milvus.py`、`db_store.py`）
- 实现类通过工厂（`infra_ai/factory.py`）或注册表（如 `ingestion/parser/registry.py`）装配
- 业务代码（service 层）只依赖 base 接口，不依赖具体实现
- 新增实现类不改现有业务代码

## 禁止行为
- ❌ **base 接口禁止 import persistence**（重点）
- ❌ base 接口禁止 import infra_ai 的具体实现
- ❌ base 接口禁止 import rag/ingestion 的具体实现
- ❌ base 接口禁止 import api/schemas
- ❌ 禁止业务代码直接依赖具体实现类（必须依赖 base 接口）
- ❌ 禁止在 base 接口中写实现逻辑（只定义签名）

## 硬约束（重点接口依赖）

| 接口 | 位置 | 允许依赖 | 禁止依赖 |
|---|---|---|---|
| BaseDocumentParser | `ingestion/parser/base.py` | domain.dto.ParsedDocument | persistence |
| BaseChunker | `ingestion/chunker/base.py` | domain.dto.ChunkDraft / domain.value_objects.ChunkConfig | persistence |
| BaseEmbeddingClient | `infra_ai/embedding/base.py` | domain | persistence |
| BaseVectorStore | `rag/vector/base.py` | domain.dto.VectorRecord / VectorHit | **persistence** |
| BaseRetriever | `rag/retrieve/base.py` | domain.dto.RetrievalResult / domain.value_objects.RetrievalQuery | **persistence** |
| BasePostProcessor | `rag/retrieve/postprocessor/base.py` | domain.dto.RetrievalResult | **persistence** |
| BaseReranker | `infra_ai/rerank/base.py` | domain.dto.RetrievalResult | persistence |
| BaseLLMClient | `infra_ai/llm/base.py` | domain.dto.ChatChunk / domain.value_objects.ChatOptions | persistence |
| **BaseMemoryStore** | `rag/memory/base.py` | **只依赖 domain.dto / domain.value_objects** | **persistence（重点）** |

## 实现类依赖豁免
实现类（非 base 接口）允许依赖 persistence：
- `rag/memory/db_store.py`（DbMemoryStore）→ 可依赖 persistence.repositories
- `rag/vector/milvus.py`（MilvusVectorStore）→ 可依赖 infra_ai / domain
- `rag/retrieve/vector_retriever.py`（VectorRetriever）→ 可依赖 infra_ai + rag.vector
- `infra_ai/embedding/openai_compat.py` → 可依赖 domain + httpx
- `infra_ai/llm/openai_compat.py` → 可依赖 domain + httpx

## 依赖关系示意（以 BaseMemoryStore 为例）
```
rag/memory/base.py          (BaseMemoryStore)  ──► domain.dto / domain.value_objects
                                                ✗ persistence（禁止）

rag/memory/db_store.py      (DbMemoryStore)    ──► domain + persistence.repositories
```
