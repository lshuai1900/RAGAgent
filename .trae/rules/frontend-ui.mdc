---
description: 前端 UI 中文与工程控制台风格规范
globs: "web/**"
---

# frontend-ui

## 适用范围
`web/` 前端工程的所有页面、组件、布局文件。

## 中文 UI（最高优先级）
- 所有**用户可见文案**必须使用简体中文
- 必须中文的范围：菜单、页面标题、按钮、表单 label、placeholder、校验提示、loading 文案、成功/错误提示、空状态、状态标签、表格列名、引用来源说明、弹窗标题与确认/取消按钮
- 保持英文的范围：代码变量名、文件名、类型名、接口路径、状态机枚举值（仅作内部数据）、trace_id 值

## 状态文案映射（硬约束）
前端**禁止**把后端枚举值原样展示，必须经映射：

### 文档状态
| 后端值 | 中文 | Tag 颜色 |
|---|---|---|
| `pending` | 待处理 | default |
| `parsing` | 解析中 | processing |
| `chunking` | 分块中 | processing |
| `embedding` | 向量化中 | processing |
| `indexing` | 入库中 | processing |
| `completed` | 已完成 | success |
| `failed` | 失败 | error |

未知状态兜底："未知状态"，Tag 用 default。

### 知识库状态
| 后端值 | 中文 | Tag 颜色 |
|---|---|---|
| `active` | 启用 | success |
| `archived` | 已归档 | default |

## 工程控制台风格
- 浅色背景（`#f5f5f5` 内容区 / `#ffffff` 卡片）
- 细边框（1px solid `#f0f0f0`），无阴影或极轻阴影
- 统一圆角 6px
- 主色用 Ant Design Vue 默认 `#1677ff`，不自定义品牌色
- 高信息密度：表格紧凑（small size）、卡片网格
- 字体：系统无衬线，正文 14px，标题 16-20px
- 间距：8px 栅格

## 仿 Yuxi 的信息架构（只参考结构，不复制源码）
- 知识库列表页：卡片网格 + 新建弹窗 + 点击进入详情
- 知识库详情页：顶部基本信息 + 标签页（文件管理/检索测试/聊天问答/配置）
- 文件管理：上传 + 状态统计 + 表格 + 状态轮询
- 聊天问答：选知识库 + 输入问题 + SSE 流式输出 + 引用来源
- 设置：后端 API 地址配置

## 禁止行为
- ❌ 禁止营销页风格（大图 Banner、渐变背景、插画、口号）
- ❌ 禁止中英混杂的可见文案（如"创建 KB""Upload 文档""Loading…"）
- ❌ 禁止把后端枚举值原样展示给用户
- ❌ 禁止把英文错误信息原样抛给用户（必须中文 + 追踪编号）
- ❌ 禁止过重阴影、大圆角（>8px）、玻璃拟态
- ❌ 禁止复制 Yuxi 仓库的源码、组件命名、CSS 类名、设计 Token
- ❌ 禁止引入额外 UI 框架（Element Plus / Naive UI / Vuetify）

## 错误提示规范
- 成功：`message.success`（中文，如"知识库创建成功"）
- 失败：`message.error`（中文 message + "追踪编号：{trace_id}"，trace_id 非空时）
- 网络异常：`message.error("连接后端失败，请检查 API 地址设置")`

## 加载态规范
- 列表/表格加载：`Spin` + "加载中…"
- 流式问答首包：助手区显示"正在思考…"，首个 delta 到达后替换
