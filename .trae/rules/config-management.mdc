---
description: 配置与密钥管理规范
globs: "configs/config.yaml,src/ragent/framework/core/config.py,.env.example"
---

# config-management

## 适用范围
- `configs/config.yaml`（主配置文件）
- `src/ragent/framework/core/config.py`（配置加载）
- `.env.example`（环境变量样例）
- `scripts/init_model_provider.py`（供应商初始化）

## 允许行为
- 使用 pydantic-settings 加载配置
- 配置来源优先级：环境变量 > `configs/config.yaml` > 代码默认值
- 配置项类型安全（用 Pydantic BaseModel 定义配置结构）
- 使用 `.env` 文件管理本地开发环境变量（不入 git）
- 使用 `.env.example` 提供占位样例（入 git）

## 禁止行为
- ❌ **禁止密钥入库明文**（`ModelProvider.api_key_ref` 只存引用名，实际密钥从环境变量读）
- ❌ 禁止在代码中硬编码配置值（必须走 config.yaml 或环境变量）
- ❌ 禁止在 `.env.example` 中填写真实密钥（只占位）
- ❌ 禁止 `.env` 入 git（.gitignore 必须包含 .env）
- ❌ 禁止配置文件包含注释外的敏感信息
- ❌ 禁止在日志中输出配置中的密钥字段

## 硬约束
- **配置加载**：`framework/core/config.py` 用 pydantic-settings 的 `BaseSettings` + YAML loader
- **环境变量覆盖**：所有配置项支持环境变量覆盖（命名 `RAGENT__{SECTION}__{KEY}`）
- **密钥引用**：`ModelProvider.api_key_ref` 存引用名（如 `QWEN_API_KEY`），实际密钥从环境变量 `QWEN_API_KEY` 读取
- **配置结构**：
  ```yaml
  app:
    host: 0.0.0.0
    port: 8000
  db:
    url: postgresql+asyncpg://...
  milvus:
    uri: http://localhost:19530
  llm:
    default_provider: qwen
  embedding:
    default_provider: qwen
  rag:
    default_top_k: 10
  ```
- **`.env.example` 必含**：DB_URL / MILVUS_URI / QWEN_API_KEY / QWEN_BASE_URL / LLM_MODEL / EMBEDDING_MODEL
