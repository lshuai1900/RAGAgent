---
description: 前端分批推进纪律（P1.0 - P1.5）
globs: "web/**"
---

# frontend-task-batch

## 适用范围
`web/` 前端工程的所有编码工作，约束 P1.0-P1.5 批次推进节奏。

## 批次划分

| 批次 | 范围 |
|---|---|
| P1.0 | 前端设计与规则落地（仅文档与规则，零业务代码） |
| P1.1 | Vue 项目骨架 + 布局 + 中文菜单 |
| P1.2 | 知识库列表页 + 新建知识库 |
| P1.3 | 知识库详情页 + 文件管理 + 上传文档 |
| P1.4 | 聊天问答 + POST SSE 流式输出 |
| P1.5 | 联调、中文 UI 验收、README 更新 |

## 当前批次
**当前批次：P1.0 — 前端设计与规则落地。**

P1.0 只产出文档与规则，**不写任何前端业务代码**，不创建 `package.json` / Vue 文件 / TS 业务代码。

## 允许行为
- 严格按 P1.0 → P1.5 顺序推进
- 每批完成后对照 `docs/FRONTEND_ACCEPTANCE_CRITERIA.md` 对应批次验收
- 验收通过 + 用户确认后才启动下一批
- 每批完成后输出新增文件清单 + 验收结果

## 禁止行为
- ❌ **禁止一次性实现所有前端页面**（必须分批）
- ❌ **禁止提前实现后续批次功能**（如 P1.1 不得写知识库列表）
- ❌ **禁止实现暂缓项**（见下方清单）
- ❌ 禁止跨批次提交
- ❌ 禁止跳过验收直接进入下一批

## 硬约束
- **当前批次确认**：每次编码开始前必须确认当前批次编号
- **范围拦截**：用户要求实现超出当前批次范围的功能 → 主动拒绝并提示当前批次范围
- **暂缓项拦截**：用户要求实现暂缓项 → 立即停止并说明

## 暂缓项清单（前端 MVP 内禁止实现）
- 用户登录 / 权限管理
- 多知识库路由选择
- Rerank 模型配置界面
- 对话标题自动生成
- 文档删除 / 重新摄取（后端无接口）
- 知识库编辑 / 删除（后端无接口）
- 对话历史列表查询（后端未暴露 list 路由）
- 暗色主题
- 国际化（i18n，MVP 锁定简体中文）
- 移动端适配
- 自动化类型生成（OpenAPI → TypeScript）
- SSR / Nuxt / PWA / 微前端
- 引用来源真实渲染（依赖后端 done 事件扩展 citations）

## 各批开始条件
| 批次 | 开始条件 |
|---|---|
| P1.0 | 后端 MVP 验收通过 + 用户确认进入前端阶段 |
| P1.1 | P1.0 验收通过 + 用户确认 |
| P1.2 | P1.1 验收通过 + 用户确认 |
| P1.3 | P1.2 验收通过 + 用户确认 |
| P1.4 | P1.3 验收通过 + 用户确认 |
| P1.5 | P1.4 验收通过 + 用户确认 |
