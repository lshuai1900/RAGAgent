---
description: 分层架构与依赖方向硬约束
globs: "src/ragent/**/*.py"
---

# layered-architecture

## 适用范围
所有 `src/ragent/` 下的源文件，约束分层与依赖方向。

## 分层结构
```
api  ──►  service  ──►  { domain, persistence, infra_ai, rag, ingestion, schemas }
所有层 ──►  framework
```

## 允许的依赖方向
| 层 | 允许依赖 |
|---|---|
| `api/` | service, schemas, framework |
| `service/` | domain, persistence, infra_ai, rag, ingestion, schemas, framework |
| `schemas/` | domain, framework |
| `persistence/` | domain, framework |
| `infra_ai/` | domain, framework |
| `rag/` | domain, infra_ai, persistence(仅实现类), framework |
| `ingestion/` | domain, infra_ai, persistence, framework |
| `domain/` | (无，纯领域) |
| `framework/` | (无业务依赖) |

## 禁止的依赖（硬约束）
- ❌ **domain 不得 import** schemas / persistence / infra_ai / rag / ingestion / api / framework
- ❌ **schemas 不得被 domain 依赖**（schemas 可依赖 domain，反向禁止）
- ❌ **api 不得直接调用** repository / infra_ai / rag / ingestion（只能调 service）
- ❌ **framework 不得 import** 任何业务层（domain/persistence/infra_ai/rag/ingestion/api/schemas）
- ❌ **base 接口不得 import persistence**（重点见 abstraction-interface 规则）

## 各层职责硬约束

### domain/（纯领域）
- 只允许包含三个文件：`enums.py`、`dto.py`、`value_objects.py`
- 零业务依赖，不 import 任何其他业务层
- 不含 ORM Model，不含 Pydantic Schema，不含业务逻辑

### schemas/（顶层独立）
- Pydantic v2 API Schema（请求/响应模型、ApiResponse[T]、分页）
- 顶层独立，不属于 domain
- 可依赖 domain（用枚举/DTO）
- 不得 import persistence / infra_ai / rag / ingestion / api

### persistence/（持久化层）
- `models/` 是 ORM Model 唯一存放位置（禁止散落他处）
- `repositories/` 是 Repository 数据访问层
- 依赖 domain（用 DTO/枚举）
- ORM Model 与 Pydantic Schema 严禁混用

### service/（唯一编排中心）
- 业务用例的唯一入口
- 编排 domain/persistence/infra_ai/rag/ingestion
- 不含 HTTP 语义（不返回 Response 对象，不感知 status_code）

### api/（路由层）
- 只调用 service
- 使用 schemas 做请求/响应
- 不含业务逻辑

## 实现类依赖豁免
实现类（非 base 接口）允许依赖 persistence：
- `rag/memory/db_store.py`（DbMemoryStore）可依赖 persistence.repositories
- `rag/vector/milvus.py`（MilvusVectorStore）可依赖 infra_ai/domain
- `rag/retrieve/vector_retriever.py` 可依赖 infra_ai + rag.vector
- `ingestion/parser/*.py`、`ingestion/chunker/*.py` 可依赖 domain
